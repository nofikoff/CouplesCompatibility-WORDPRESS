{% extends "layouts/base.twig" %}

{% block content %}
<section class="min-h-screen bg-gradient-to-br from-purple-900 via-purple-800 to-purple-700 flex items-center justify-center">
    <div class="max-w-4xl mx-auto px-4 py-24 text-center">
        <h1 class="text-8xl font-bold text-white mb-4">404</h1>
        <p class="text-2xl text-purple-200 mb-8">{{ __('Page not found', 'couplescompatibility') }}</p>
        <a href="{{ site.url }}" class="inline-block bg-yellow-400 hover:bg-yellow-500 text-purple-900 font-semibold px-8 py-4 rounded-lg transition-colors">
            {{ __('Go to Homepage', 'couplescompatibility') }}
        </a>
    </div>
</section>
{% endblock %}
