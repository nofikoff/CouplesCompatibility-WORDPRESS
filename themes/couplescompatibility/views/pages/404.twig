{% extends "layouts/base.twig" %}

{% block content %}
<div class="max-w-4xl mx-auto px-4 py-24 text-center">
    <h1 class="text-6xl font-bold text-gray-900 mb-4">404</h1>
    <p class="text-xl text-gray-600 mb-8">{{ __('Page not found', 'couplescompatibility') }}</p>
    <a href="{{ site.url }}" class="inline-block bg-pink-500 hover:bg-pink-600 text-white font-semibold px-6 py-3 rounded-lg transition-colors">
        {{ __('Go to Homepage', 'couplescompatibility') }}
    </a>
</div>
{% endblock %}
